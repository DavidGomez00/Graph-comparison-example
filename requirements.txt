# Curated for this repo's own scripts only (not a full environment freeze).
# Verified against every `import`/`from` in *.py at the repo root.
grakel==0.1.11
matplotlib==3.10.8
networkx==3.4.2
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
